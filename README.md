# TVRemotecontroller

This is a TV Remote Controller has all the functionality that shown on the app.  Run it with live-server which is an extension for VSCode that you have to install it in case if you have not installed yet.  
